import React from 'react';
import {Text, View} from 'react-native';

type Props = {};

export default function Calendar({}: Props) {
  return (
    <View>
      <Text>Calendar</Text>
    </View>
  );
}
